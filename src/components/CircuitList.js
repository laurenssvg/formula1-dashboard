import React from "react";
import styled from "styled-components";

const CircuitList = () => {
  return <Wrapper>CircuitList</Wrapper>;
};

const Wrapper = styled.section`
  display: grid;
  place-items: center;
`;

export default CircuitList;
