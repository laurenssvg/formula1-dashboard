import React from "react";
import styled from "styled-components";

const SeasonList = () => {
  return <Wrapper>SeasonList</Wrapper>;
};

const Wrapper = styled.section`
  display: grid;
  place-items: center;
`;

export default SeasonList;
